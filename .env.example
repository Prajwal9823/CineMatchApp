# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/cinematch

# TMDB API Configuration
TMDB_API_KEY=your_tmdb_api_key_here

# Session Configuration (generate a random string for production)
SESSION_SECRET=your_session_secret_here

# Replit Auth Configuration (optional - for authentication features)
REPL_ID=your_repl_id
ISSUER_URL=https://replit.com/oidc
REPLIT_DOMAINS=localhost:5000

# PostgreSQL Connection Details (alternative format)
PGHOST=localhost
PGPORT=5432
PGDATABASE=cinematch
PGUSER=your_username
PGPASSWORD=your_password